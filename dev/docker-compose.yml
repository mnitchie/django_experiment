version: '3.7'

services:
  django_experiment-dev:
    container_name: django_experiment-dev
    restart: always
    build:
      context: ../
      dockerfile: dev/Dockerfile
      
    stdin_open: true
    tty: true
    volumes:
      - "../:/usr/src/app"
